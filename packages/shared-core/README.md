# RhymeLab Shared Core

Framework-independent product behavior extracted from the proven Studio implementation.

This package deliberately contains no React components and no Node filesystem/runtime ownership. Browser-only persistence and environment probes live in `packages/platform-web`.

See `docs/SHARED_CORE_ARCHITECTURE.md`.
