# RhymeLab Web Platform Adapters

Browser-specific adapters for the Shared Core: IndexedDB, LocalStorage compatibility, VisualViewport, diagnostics and device-environment support.

These modules are replaceable by Electron and mobile platform adapters. They must not become domain authority.

See `docs/SHARED_CORE_ARCHITECTURE.md`.
